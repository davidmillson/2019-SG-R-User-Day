# Slide 9
#
# Here's an example of an interactions map for the far more
# complicated GDP app. The principle is the same - know what
# the code is supposed to do.

tabPanel(
  "9",
  img(src = "gdp_map.png",
      style = "height:auto;width:100%")
)